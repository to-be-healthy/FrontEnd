'use client';

import styled from 'styled-components';

const StyleDiv = styled.div`
  background-color: skyblue;
`;

export default function Page() {
  return (
    <main>
      <StyleDiv>메인 페이지</StyleDiv>
    </main>
  );
}
